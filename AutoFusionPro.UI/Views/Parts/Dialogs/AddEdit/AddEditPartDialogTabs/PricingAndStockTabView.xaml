<UserControl x:Class="AutoFusionPro.UI.Views.Parts.Dialogs.AddEdit.AddEditPartDialogTabs.PricingAndStockTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:AutoFusionPro.UI.ViewModels.Parts.Dialogs.AddEdit"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             xmlns:xBtn="clr-namespace:AutoFusionPro.UI.Controls.Buttons"
             xmlns:spinner="clr-namespace:AutoFusionPro.UI.Controls.LoadingSpinners"
             xmlns:behaviors="clr-namespace:AutoFusionPro.UI.Resources.Behaviors"
             xmlns:wpfUi="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:AutoFusionPro.UI.Views.Parts.Dialogs.AddEdit.AddEditPartDialogTabs"
             xmlns:forms="clr-namespace:AutoFusionPro.UI.Controls.Forms"
             xmlns:cards="clr-namespace:AutoFusionPro.UI.Controls.Cards"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignWidth="800"
             Background="{DynamicResource Card.Header.BackgroundBrush}"
             Foreground="{DynamicResource Text.PrimaryBrush}"
             d:DataContext="{d:DesignInstance Type=vm:AddEditPartDialogViewModel}"
             FontFamily="{DynamicResource DynamicFontFamilyRegular}"
             FlowDirection="{Binding CurrentWorkFlow}">
    <Border Padding="8">
        <ScrollViewer Padding="8,0">
            <StackPanel>
                <!-- Cost Price & Selling Price-->
                <Grid Margin="8,20,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Cost Price -->
                    <StackPanel ToolTip="{DynamicResource CostPriceTooltipMessageStr}">
                        <StackPanel Orientation="Horizontal">
                            <wpfUi:SymbolIcon Symbol="Money24"
                                              VerticalAlignment="Center" FontSize="16"/>
                            <TextBlock Text="{DynamicResource CostPriceStr}"
                                       Margin="5,0,0,0"
                                       FontFamily="{DynamicResource DynamicFontFamilyBold}"
                                       VerticalAlignment="Center"
                                       ToolTip="{DynamicResource CostPriceTooltipMessageStr}" />
                            <TextBlock Margin="5,0,0,0"
                                       FontFamily="{DynamicResource DynamicFontFamilyMedium}" Foreground="{DynamicResource Text.SecondaryBrush}"
                                       VerticalAlignment="Center">
                                <Run Text="(" />
                                <Run Text="{DynamicResource InStr}" />
                                <Run Text="{Binding CurrentCurrencyFullName}" />
                                <Run Text=")" />
                            </TextBlock>
                        </StackPanel>
                        <wpfUi:NumberBox x:Name="CostPriceNumberBox" 
                                         Value="{Binding CostPrice, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                         Minimum="0"
                                         Margin="0,8,0,0"
                                         PlaceholderText="{DynamicResource CostPriceExampleStr }"
                                         Style="{DynamicResource ValidatedNumberBoxStyle}"
                                         ToolTip="{DynamicResource CostPriceTooltipMessageStr}" />

                        <!-- Error Message TextBlock -->
                        <TextBlock Text="{Binding ElementName=CostPriceNumberBox, Path=(Validation.Errors), Converter={StaticResource FirstValidationErrorConverter}}"
                                   Foreground="{DynamicResource Border.ErrorBrush}"
                                   FontSize="12"
                                   Margin="0,2,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <!-- Hide by default -->
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                    <!-- Show only when the associated control has an error -->
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=CostPriceNumberBox, Path=(Validation.HasError)}"
                                                     Value="True">
                                            <Setter Property="Visibility"
                                                    Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                    </StackPanel>

                    <!-- Selling Price -->
                    <StackPanel Grid.Column="2">
                        <StackPanel Orientation="Horizontal">
                            <wpfUi:SymbolIcon Symbol="Money24"
                                              VerticalAlignment="Center"
                                              FontSize="16" />
                            <TextBlock Text="{DynamicResource SellingPriceStr}"
                                       VerticalAlignment="Center"
                                       Margin="5,0,0,0"
                                       FontFamily="{DynamicResource DynamicFontFamilyBold}"
                                       ToolTip="{DynamicResource SellingPriceTooltipMessageStr}" />
                            <TextBlock Margin="5,0,0,0"
                                       FontFamily="{DynamicResource DynamicFontFamilyMedium}"
                                       Foreground="{DynamicResource Text.SecondaryBrush}"
                                       VerticalAlignment="Center">
                                <Run Text="(" />
                                <Run Text="{DynamicResource InStr}" />
                                <Run Text="{Binding CurrentCurrencyFullName}" />
                                <Run Text=")" />
                            </TextBlock>
                        </StackPanel>
                        <wpfUi:NumberBox Value="{Binding SellingPrice, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                         x:Name="sellingPriceNumberBox"
                                         Minimum="0"
                                         Style="{DynamicResource ValidatedNumberBoxStyle}"
                                         Margin="0,8,0,0"
                                         PlaceholderText="{DynamicResource SellingPriceExampleStr}"
                                         ToolTip="{DynamicResource SellingPriceTooltipMessageStr}" />
                        <!-- Error Message TextBlock -->
                        <TextBlock Text="{Binding ElementName=sellingPriceNumberBox, Path=(Validation.Errors), Converter={StaticResource FirstValidationErrorConverter}}"
                                   Foreground="{DynamicResource Border.ErrorBrush}"
                                   FontSize="12"
                                   Margin="0,2,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <!-- Hide by default -->
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                    <!-- Show only when the associated control has an error -->
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=sellingPriceNumberBox, Path=(Validation.HasError)}"
                                                     Value="True">
                                            <Setter Property="Visibility"
                                                    Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>


                </Grid>

                <!-- Initial Stock, Reorder level-->
                <Grid Margin="8,20,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Reorder level -->
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <wpfUi:SymbolIcon Symbol="NumberSymbol48"
                                              VerticalAlignment="Center" />
                            <TextBlock Text="{DynamicResource ReorderLevelStr}"
                                       Margin="5,0,0,0"
                                       FontFamily="{DynamicResource DynamicFontFamilyBold}"
                                       VerticalAlignment="Center"
                                       ToolTip="{DynamicResource ReorderLevelToolTipStr}" />
                        </StackPanel>
                        <wpfUi:NumberBox Value="{Binding ReorderLevel, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                         Minimum="0"
                                         x:Name="ReorderLevelNumberBox"
                                         Margin="0,8,0,0"
                                         Style="{DynamicResource ValidatedNumberBoxStyle}"
                                         PlaceholderText="{DynamicResource ReorderLevelPlaceholderStr }"
                                         ToolTip="{DynamicResource ReorderLevelToolTipStr}" />
                        <!-- Error Message TextBlock -->
                        <TextBlock Text="{Binding ElementName=ReorderLevelNumberBox, Path=(Validation.Errors), Converter={StaticResource FirstValidationErrorConverter}}"
                                   Foreground="{DynamicResource Border.ErrorBrush}"
                                   FontSize="12"
                                   Margin="0,2,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <!-- Hide by default -->
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                    <!-- Show only when the associated control has an error -->
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=ReorderLevelNumberBox, Path=(Validation.HasError)}"
                                                     Value="True">
                                            <Setter Property="Visibility"
                                                    Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                    </StackPanel>

                    <!-- Minimum Stock -->
                    <StackPanel Grid.Column="2">
                        <StackPanel Orientation="Horizontal">
                            <wpfUi:SymbolIcon Symbol="NumberSymbol48"
                                              VerticalAlignment="Center" />
                            <TextBlock Text="{DynamicResource MinimumStockStr}"
                                       VerticalAlignment="Center"
                                       Margin="5,0,0,0"
                                       FontFamily="{DynamicResource DynamicFontFamilyBold}"
                                       ToolTip="{DynamicResource MinimumStockToolTipStr}" />
                        </StackPanel>
                        <wpfUi:NumberBox Value="{Binding MinimumStock, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                         Minimum="0"
                                         x:Name="MinimumStockNumberBox"
                                         Margin="0,8,0,0"
                                         Style="{DynamicResource ValidatedNumberBoxStyle}"
                                         PlaceholderText="{DynamicResource MinimumStockPlaceholderStr}"
                                         ToolTip="{DynamicResource MinimumStockToolTipStr}" />

                        <!-- Error Message TextBlock -->
                        <TextBlock Text="{Binding ElementName=MinimumStockNumberBox, Path=(Validation.Errors), Converter={StaticResource FirstValidationErrorConverter}}"
                                   Foreground="{DynamicResource Border.ErrorBrush}"
                                   FontSize="12"
                                   Margin="0,2,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <!-- Hide by default -->
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                    <!-- Show only when the associated control has an error -->
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=MinimumStockNumberBox, Path=(Validation.HasError)}"
                                                     Value="True">
                                            <Setter Property="Visibility"
                                                    Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <!-- Initial Stock -->
                    <StackPanel Grid.Column="4"
                                Visibility="{Binding IsEditing, Converter={StaticResource BooleanToInverseVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <!-- TODO : Change the currency dynamically-->
                            <wpfUi:SymbolIcon Symbol="NumberSymbol48"
                                              VerticalAlignment="Center" />
                            <TextBlock Text="{DynamicResource InitialStockQuantityStr}"
                                       VerticalAlignment="Center"
                                       Margin="5,0,0,0"
                                       FontFamily="{DynamicResource DynamicFontFamilyBold}"
                                       ToolTip="{DynamicResource InitialStockQuantityTooltipMessageStr}" />
                        </StackPanel>
                        <wpfUi:NumberBox Value="{Binding InitialStockQuantity, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                         Minimum="0"
                                         x:Name="InitialStockNumberBox"
                                         Margin="0,8,0,0"
                                         Style="{DynamicResource ValidatedNumberBoxStyle}"
                                         PlaceholderText="{DynamicResource InitialStockQuantityPlaceholderStr}"
                                         ToolTip="{DynamicResource InitialStockQuantityTooltipMessageStr}" />

                        <!-- Error Message TextBlock -->
                        <TextBlock Text="{Binding ElementName=InitialStockNumberBox, Path=(Validation.Errors), Converter={StaticResource FirstValidationErrorConverter}}"
                                   Foreground="{DynamicResource Border.ErrorBrush}"
                                   FontSize="12"
                                   Margin="0,2,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <!-- Hide by default -->
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                    <!-- Show only when the associated control has an error -->
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=InitialStockNumberBox, Path=(Validation.HasError)}"
                                                     Value="True">
                                            <Setter Property="Visibility"
                                                    Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>


                    </StackPanel>
                </Grid>

                <GroupBox Margin="0,20,0,0"
                          Padding="8,10">
                    <GroupBox.Header>
                        <TextBlock Text="{DynamicResource UnitsOfMeasuresStr}"
                                   FontFamily="{DynamicResource DynamicFontFamilyBold}" />
                    </GroupBox.Header>
                    <StackPanel>
                        <!-- Stocking Unit of Measure-->
                        <Grid Width="400"
                              HorizontalAlignment="Left">
                            <ComboBox ItemsSource="{Binding AvailableUnitOfMeasures}"
                                      x:Name="stockingUOMCombo"
                                      SelectedItem="{Binding SelectedStockingUnitOfMeasure, ValidatesOnNotifyDataErrors=True}"
                                      Panel.ZIndex="0"
                                      Style="{DynamicResource ValidatedComboboxStyle}"
                                      ToolTip="{DynamicResource StockingUnitOfMeasureToolTipStr}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <wpfUi:TextBlock Text="{DynamicResource SelectUnitStr}"
                                             IsHitTestVisible="False"
                                             FontFamily="{DynamicResource DynamicFontFamilyMedium}"
                                             FontSize="14"
                                             Padding="8,5"
                                             Margin="5,0,0,0"
                                             VerticalAlignment="Center"
                                             Foreground="{DynamicResource GrayBrush}"
                                             Panel.ZIndex="1">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility"
                                                Value="Hidden" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=stockingUOMCombo,Path=SelectedItem}"
                                                         Value="{x:Null}">
                                                <Setter Property="Visibility"
                                                        Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </wpfUi:TextBlock>
                        </Grid>

                        <!-- Error Message TextBlock -->
                        <TextBlock Text="{Binding ElementName=stockingUOMCombo, Path=(Validation.Errors), Converter={StaticResource FirstValidationErrorConverter}}"
                                   Foreground="{DynamicResource Border.ErrorBrush}"
                                   FontSize="12"
                                   Margin="0,2,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <!-- Hide by default -->
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                    <!-- Show only when the associated control has an error -->
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=stockingUOMCombo, Path=(Validation.HasError)}"
                                                     Value="True">
                                            <Setter Property="Visibility"
                                                    Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Info Label -->
                        <Border Padding="10,12"
                                Margin="0,20,0,0"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Border.BorderBrush>
                                <DrawingBrush Viewport="0,0,8,8"
                                              ViewportUnits="Absolute"
                                              TileMode="Tile">
                                    <DrawingBrush.Drawing>
                                        <DrawingGroup>
                                            <GeometryDrawing Brush="{DynamicResource Border.AccentBrush}">
                                                <GeometryDrawing.Geometry>
                                                    <GeometryGroup>
                                                        <RectangleGeometry Rect="0,0,50,50" />
                                                        <RectangleGeometry Rect="50,50,50,50" />
                                                    </GeometryGroup>
                                                </GeometryDrawing.Geometry>
                                            </GeometryDrawing>
                                        </DrawingGroup>
                                    </DrawingBrush.Drawing>
                                </DrawingBrush>
                            </Border.BorderBrush>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="15" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal">
                                    <wpfUi:SymbolIcon Symbol="Info24"
                                                      Foreground="{DynamicResource Border.AccentBrush}"
                                                      VerticalAlignment="Center" />
                                    <TextBlock Text="{DynamicResource UnitOfMeasureInfoStr}"
                                               Margin="8,0,0,0"
                                               VerticalAlignment="Center"
                                               FontSize="{StaticResource FontSizeMedium}"
                                               FontFamily="{DynamicResource DynamicFontFamilyBold}"
                                               Foreground="{DynamicResource Border.AccentBrush}" />
                                </StackPanel>

                                <TextBlock Text="{DynamicResource StockingUOMInfoStr}"
                                           Grid.Column="2"
                                           Margin="8,0,0,0"
                                           VerticalAlignment="Center"
                                           TextWrapping="Wrap"
                                           FontFamily="{DynamicResource DynamicFontFamilyRegular}"
                                           Foreground="{DynamicResource Border.AccentBrush}" />
                            </Grid>
                        </Border>

                        <Grid Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="15" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- Sales Unit of Measure -->
                            <StackPanel>
                                <TextBlock Text="{DynamicResource SalesUnitOfMeasureStr}"
                                           VerticalAlignment="Center"
                                           FontFamily="{DynamicResource DynamicFontFamilyBold}" />
                                <!-- Sales Unit of Measure-->
                                <Grid Margin="0,8,0,0">
                                    <ComboBox ItemsSource="{Binding AvailableSalesUnitOfMeasures}"
                                              x:Name="salesUOMCombo"
                                              SelectedItem="{Binding SelectedSalesUnitOfMeasure}"
                                              Panel.ZIndex="0"
                                              ToolTip="{DynamicResource StockingUnitOfMeasureToolTipStr}">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                    <wpfUi:TextBlock Text="{DynamicResource SelectUnitStr}"
                                                     IsHitTestVisible="False"
                                                     FontFamily="{DynamicResource DynamicFontFamilyMedium}"
                                                     FontSize="14"
                                                     Padding="8,5"
                                                     Margin="5,0,0,0"
                                                     VerticalAlignment="Center"
                                                     Foreground="{DynamicResource GrayBrush}"
                                                     Panel.ZIndex="1">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility"
                                                        Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=salesUOMCombo,Path=SelectedItem}"
                                                                 Value="{x:Null}">
                                                        <Setter Property="Visibility"
                                                                Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </wpfUi:TextBlock>
                                </Grid>

                                <!-- Sales Factor  -->
                                <!-- TODO Add Visibility if the selected Sales UOM is different from Stock -->
                                <StackPanel Margin="0,15,0,0"
                                            Visibility="{Binding IsSalesUoMDifferent, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Grid.Column="2">
                                    <StackPanel Orientation="Horizontal">
                                        <wpfUi:SymbolIcon Symbol="NumberSymbol48"
                                                          VerticalAlignment="Center" />
                                        <TextBlock Text="{DynamicResource SalesConversionFactorStr}"
                                                   VerticalAlignment="Center"
                                                   Margin="5,0,0,0"
                                                   FontFamily="{DynamicResource DynamicFontFamilyBold}"
                                                   ToolTip="{DynamicResource SalesConversionFactorToolTipStr}" />
                                    </StackPanel>
                                    <wpfUi:NumberBox Value="{Binding SalesConversionFactor, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True, TargetNullValue=''}"
                                                     Margin="0,8,0,0"
                                                     x:Name="SalesConversionFactorNumberBox"
                                                     Style="{DynamicResource ValidatedNumberBoxStyle}"
                                                     PlaceholderText="{DynamicResource SalesConversionFactorPlaceholderStr}"
                                                     ToolTip="{DynamicResource SalesConversionFactorToolTipStr}" />

                                    <!-- Error Message TextBlock -->
                                    <TextBlock Text="{Binding ElementName=SalesConversionFactorNumberBox, Path=(Validation.Errors), Converter={StaticResource FirstValidationErrorConverter}}"
                                               Foreground="{DynamicResource Border.ErrorBrush}"
                                               FontSize="12"
                                               Margin="0,2,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <!-- Hide by default -->
                                                <Setter Property="Visibility"
                                                        Value="Collapsed" />
                                                <!-- Show only when the associated control has an error -->
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=SalesConversionFactorNumberBox, Path=(Validation.HasError)}"
                                                                 Value="True">
                                                        <Setter Property="Visibility"
                                                                Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>

                            <!-- Purchase Unit of Measure -->
                            <StackPanel Grid.Column="2">
                                <!-- Purchase Unit of Measure-->
                                <TextBlock Text="{DynamicResource PurchaseUnitOfMeasureStr}"
                                           VerticalAlignment="Center"
                                           FontFamily="{DynamicResource DynamicFontFamilyBold}" />
                                <Grid Margin="0,8,0,0">
                                    <ComboBox ItemsSource="{Binding AvailablePurchaseUnitOfMeasures}"
                                              x:Name="purchaseUOMCombo"
                                              SelectedItem="{Binding SelectedPurchaseUnitOfMeasure}"
                                              Panel.ZIndex="0"
                                              ToolTip="{DynamicResource PurchaseUnitOfMeasureToolTipStr}">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                    <wpfUi:TextBlock Text="{DynamicResource SelectUnitStr}"
                                                     IsHitTestVisible="False"
                                                     FontFamily="{DynamicResource DynamicFontFamilyMedium}"
                                                     FontSize="14"
                                                     Padding="8,5"
                                                     Margin="5,0,0,0"
                                                     VerticalAlignment="Center"
                                                     Foreground="{DynamicResource GrayBrush}"
                                                     Panel.ZIndex="1">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility"
                                                        Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=purchaseUOMCombo,Path=SelectedItem}"
                                                                 Value="{x:Null}">
                                                        <Setter Property="Visibility"
                                                                Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </wpfUi:TextBlock>
                                </Grid>

                                <!-- Purchase Factor  -->
                                <!-- TODO Add Visibility if the selected Sales UOM is different from Stock -->
                                <StackPanel Margin="0,15,0,0"
                                            Visibility="{Binding IsPurchaseUoMDifferent, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Grid.Column="2">
                                    <StackPanel Orientation="Horizontal">
                                        <wpfUi:SymbolIcon Symbol="NumberSymbol48"
                                                          VerticalAlignment="Center" />
                                        <TextBlock Text="{DynamicResource PurchaseConversionFactorStr}"
                                                   VerticalAlignment="Center"
                                                   Margin="5,0,0,0"
                                                   FontFamily="{DynamicResource DynamicFontFamilyBold}"
                                                   ToolTip="{DynamicResource PurchaseConversionFactorToolTipStr}" />
                                    </StackPanel>
                                    <wpfUi:NumberBox Value="{Binding PurchaseConversionFactor, UpdateSourceTrigger=PropertyChanged,ValidatesOnNotifyDataErrors=True, TargetNullValue=''}"
                                                     Margin="0,8,0,0"
                                                     x:Name="PurchaseConversionFactorNumberBox"
                                                     Style="{DynamicResource ValidatedNumberBoxStyle}"
                                                     PlaceholderText="{DynamicResource PurchaseConversionFactorPlaceholderStr}"
                                                     ToolTip="{DynamicResource PurchaseConversionFactorToolTipStr}" />

                                    <!-- Error Message TextBlock -->
                                    <TextBlock Text="{Binding ElementName=PurchaseConversionFactorNumberBox, Path=(Validation.Errors), Converter={StaticResource FirstValidationErrorConverter}}"
                                               Foreground="{DynamicResource Border.ErrorBrush}"
                                               FontSize="12"
                                               Margin="0,2,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <!-- Hide by default -->
                                                <Setter Property="Visibility"
                                                        Value="Collapsed" />
                                                <!-- Show only when the associated control has an error -->
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=PurchaseConversionFactorNumberBox, Path=(Validation.HasError)}"
                                                                 Value="True">
                                                        <Setter Property="Visibility"
                                                                Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </GroupBox>

            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
