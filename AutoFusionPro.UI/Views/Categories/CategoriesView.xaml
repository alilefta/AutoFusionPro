<UserControl x:Class="AutoFusionPro.UI.Views.Categories.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="interact"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AutoFusionPro.UI.Views.Categories"
             xmlns:vm="clr-namespace:AutoFusionPro.UI.ViewModels.Categories"
             xmlns:xBtn="clr-namespace:AutoFusionPro.UI.Controls.Buttons"
             xmlns:forms="clr-namespace:AutoFusionPro.UI.Controls.Forms"
             xmlns:spinner="clr-namespace:AutoFusionPro.UI.Controls.LoadingSpinners"
             xmlns:dtos="clr-namespace:AutoFusionPro.Application.DTOs.Category;assembly=AutoFusionPro.Application"
             xmlns:wpfUi="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CategoriesViewModel}"
             d:DesignHeight="550"
             d:DesignWidth="1200"
             Background="{DynamicResource Window.BackgroundBrush}"
             FlowDirection="{Binding CurrentWorkFlow}"
             Foreground="{DynamicResource Text.PrimaryBrush}"
             FontFamily="{DynamicResource DynamicFontFamilyRegular}">
    <Border Padding="10,15,10,8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Content -->
            <Grid Margin="8,10,8,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Search, Filters and Action Buttons -->
                <Grid Margin="8,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--<wpfUi:TextBox Icon="{wpfUi:SymbolIcon Symbol=Search48}" PlaceholderText="Search for Parts By Name, Car Model, Part Number"/>-->
                    <forms:SearchTextBox SearchQuery="{Binding SearchQueryText, UpdateSourceTrigger=PropertyChanged}"
                                         PlaceholderText="{DynamicResource SearchForCompatibleVehiclesStr}"
                                         ButtonCommand="{Binding ClearSearchAndReloadCommand}"
                                         IsClearButtonEnabled="{Binding HasTextSearchQuery}"
                                         ButtonVisibility="{Binding HasTextSearchQuery, Converter={StaticResource BooleanToVisibilityConverter}}"
                                         Height="40" />
                    
                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal"
                                Grid.Column="2"
                                Margin="50,0,0,0">

                        <xBtn:XButton ButtonType="Regular"
                                      Style="{DynamicResource RegularButtonStyle}"
                                      Command="{Binding ChangeViewCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="{DynamicResource DynamicFontFamilyRegular}"
                                           VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DataContext.IsCardViewActive, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                             Value="False">
                                                    <Setter Property="Text"
                                                            Value="{DynamicResource ShowGridViewStr}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DataContext.IsCardViewActive, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                             Value="True">
                                                    <Setter Property="Text"
                                                            Value="{DynamicResource ShowListViewStr}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <wpfUi:SymbolIcon Symbol="{Binding ViewerIcon}"
                                                  Margin="5,0,0,0"
                                                  VerticalAlignment="Center">
                                </wpfUi:SymbolIcon>
                            </StackPanel>
                        </xBtn:XButton>

                        <xBtn:XButton ButtonType="Primary"
                                      IsLoading="{Binding IsAdding}"
                                      Margin="8,0,0,0"
                                      Command="{Binding ShowAddCategoryCommand}"
                                      Style="{DynamicResource RegularButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource AddCategoryStr}"
                                           VerticalAlignment="Center"
                                           FontSize="13" />
                                <wpfUi:SymbolIcon Symbol="Add48"
                                                  Margin="5,0,0,0"
                                                  VerticalAlignment="Center"
                                                  FontSize="13" />

                            </StackPanel>
                        </xBtn:XButton>
                    </StackPanel>

                </Grid>
                
                <!-- Data Grid -->
                <Grid Grid.Row="2"
                      Margin="0,10,0,0">
             
                    <wpfUi:DataGrid ItemsSource="{Binding CategoriesCollection}" Visibility="{Binding IsCardViewActive, Converter={StaticResource BooleanToInverseVisibilityConverter}}"
                                    Grid.Row="2"
                                    Margin="0,8,0,0"
                                    SelectedItem="{Binding SelectedCategory}"
                                    Style="{DynamicResource ModernDataGridStyle}"
                                    AutoGenerateColumns="False"
                                    CanUserAddRows="False"
                                    IsReadOnly="True"
                                    CanUserDeleteRows="False"
                                    CanUserReorderColumns="True"
                                    CanUserResizeColumns="True"
                                    CanUserSortColumns="True"
                                    SelectionMode="Single"
                                    GridLinesVisibility="Horizontal"
                                    AlternationCount="2"
                                    AlternatingRowBackground="{DynamicResource DataGrid.AlternatingRow.BackgroundBrush}"
                                    HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource CategoryNameStr}"
                                                Binding="{Binding Name}"
                                                Width="120" />
                            <DataGridTextColumn Header="{DynamicResource ModelStr}"
                                                Binding="{Binding IsActive}"
                                                Width="120" />
                            <DataGridTemplateColumn Header="{DynamicResource CategoryImageStr}"
                                                    Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding PartCount}">
                                            <TextBlock.Foreground>
                                                <MultiBinding Converter="{StaticResource TextWithSelectionForegroundConverter}">
                                                    <Binding Path="PartCount" />
                                                    <Binding Path="IsSelected"
                                                             RelativeSource="{RelativeSource AncestorType=DataGridRow}" />
                                                </MultiBinding>
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="{DynamicResource ManageStr}"
                                                    Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal"
                                                    HorizontalAlignment="Center">
                                            <xBtn:XButton Cursor="Hand"
                                                          Style="{DynamicResource RegularButtonStyle}"
                                                          ButtonType="Warning"
                                                          Command="{Binding DataContext.ShowEditCompatibleVehicleDialogCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                          CommandParameter="{Binding}"
                                                          Opacity="0.8"
                                                          VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal"
                                                            VerticalAlignment="Center">
                                                    <wpfUi:SymbolIcon Symbol="Edit48"
                                                                      VerticalAlignment="Center" />
                                                </StackPanel>
                                            </xBtn:XButton>

                                            <xBtn:XButton Cursor="Hand"
                                                          Style="{DynamicResource RegularButtonStyle}"
                                                          Command="{Binding DataContext.ShowDeleteCompatibleVehicleDialogCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                          CommandParameter="{Binding}"
                                                          ButtonType="Danger"
                                                          VerticalAlignment="Center"
                                                          Opacity="0.8"
                                                          Margin="5,0,0,0">
                                                <StackPanel Orientation="Horizontal"
                                                            VerticalAlignment="Center">
                                                    <wpfUi:SymbolIcon Symbol="Delete48"
                                                                      VerticalAlignment="Center" />
                                                </StackPanel>
                                            </xBtn:XButton>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </wpfUi:DataGrid>

                    <ItemsControl ItemsSource="{Binding CategoriesCollection}"
                                  Visibility="{Binding IsCardViewActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type dtos:CategoryDto}">
                                <Border x:Name="CardBorder"
                                        Width="220"
                                        Height="220"
                                        Margin="10"
                                    Background="{DynamicResource Card.BackgroundBrush}"
                    BorderBrush="{DynamicResource Card.BorderBrush}"
                    BorderThickness="1" CornerRadius="8" Effect="{StaticResource ShadowEffect}">
                                    <!--<i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseLeftButtonUp">
                                            <i:InvokeCommandAction Command="{Binding DataContext.ViewCategoryDetailsCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                                   CommandParameter="{Binding}" />
                                        </i:EventTrigger>
                                        --><!-- Hover effects for shadow or action button visibility --><!--
                                    </i:Interaction.Triggers>-->
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <!-- Image Area -->
                                            <RowDefinition Height="Auto"
                                                           MinHeight="60" />
                                            <!-- Info Area (Name, PartCount, Actions) -->
                                        </Grid.RowDefinitions>

                                        <!-- Category Image -->
                                        <Border Grid.Row="0"
                                                CornerRadius="7,7,0,0"
                                                ClipToBounds="True">
                                            <Image Source="{Binding ImagePath, Converter={StaticResource StringToImageSourceConverter}}"
                                                   Stretch="UniformToFill" />
                                        </Border>

                                        <!-- Info Overlay/Footer -->
                                        <Border Grid.Row="1"
                                                Background="{DynamicResource Card.BackgroundBrush}"
                                            Padding="10">
                                            <StackPanel>
                                                <!-- Category Name -->
                                                <TextBlock Text="{Binding Name}"
                                                           FontWeight="SemiBold"
                                                           FontSize="16"
                                                           Foreground="{DynamicResource Card.Header.ForegroundBrush}"
                                                           TextWrapping="WrapWithOverflow"
                                                           TextTrimming="CharacterEllipsis"
                                                           MaxHeight="40" />
                                                <!-- Limit to 2 lines approx -->

                                                <!-- Part Count -->
                                                <TextBlock Margin="0,4,0,0"
                                                           FontSize="12"
                                                           Foreground="{DynamicResource Card.Body.ForegroundBrush}"
                                                           Opacity="0.8">
                                <Run Text="{Binding PartCount}" />
                                <Run Text="Parts" /> <!-- Or localize "Parts" -->
                                                </TextBlock>

                                                <!-- Optional: Has Subcategories Indicator -->
                                                <wpfUi:SymbolIcon Symbol="FolderOpenVertical20"
                                                                  FontSize="12"
                                                                  Foreground="{DynamicResource Text.SecondaryBrush}"
                                                                  Opacity="0.7"
                                                                  Visibility="{Binding HasChildren, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                  ToolTip="Has subcategories"
                                                                  Margin="0,2,0,0" />

                                                <!-- Optional: Quick Action Buttons (e.g., on hover of the card) -->
                                                <StackPanel Orientation="Horizontal"
                                                            HorizontalAlignment="Right"
                                                            Margin="0,8,0,0"
                                                            Visibility="{Binding IsMouseOver, ElementName=CardBorder, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <Button ToolTip="Edit Category"
                                                            Command="{Binding DataContext.EditCategoryCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{DynamicResource IconButtonStyle}">
                                                        <!-- Assuming a style for icon-only buttons -->
                                                        <wpfUi:SymbolIcon Symbol="Edit24"
                                                                          FontSize="16" />
                                                    </Button>
                                                    <Button ToolTip="Delete Category"
                                                            Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{DynamicResource IconButtonStyle}"
                                                            Margin="5,0,0,0">
                                                        <wpfUi:SymbolIcon Symbol="Delete24"
                                                                          FontSize="16" />
                                                    </Button>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <spinner:DonutSpinner Width="200"
                                          Height="200"
                                          Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}" />
                </Grid>
                
                
            </Grid>

            <!-- Status Bar -->
            <StatusBar Grid.Row="1">
                <StatusBarItem>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource TotalItemsStr}"
                                   FontFamily="{DynamicResource DynamicFontFamilyRegular}"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="{Binding CompatibleVehiclesCollection.Count}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0"
                                   FontFamily="{DynamicResource DynamicFontFamilyRegular}" />
                    </StackPanel>
                </StatusBarItem>
                <Separator />
                <StatusBarItem>
                    <ProgressBar Width="100"
                                 Height="15"
                                 IsIndeterminate="{Binding IsLoading}"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </StatusBarItem>
            </StatusBar>
        </Grid>

    </Border>
</UserControl>